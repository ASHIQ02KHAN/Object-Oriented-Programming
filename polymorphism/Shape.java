package polymorphism;                          // Declares that this class belongs to the 'polymorphism' package

public class Shape {                           // Defines a public base (parent) class named 'Shape'

    void draw() {                              // Defines a method named draw() with default implementation
        System.out.println("Drawing a shape"); // Prints a generic message for drawing a shape
    }
}

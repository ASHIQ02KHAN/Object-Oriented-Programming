package inheritance;                         // Declares that this class belongs to the 'inheritance' package

public class Animal {                        // Defines a public class named 'Animal'

    void sound() {                           // Declares a method named 'sound' with no return value
        System.out.println("Animal makes a sound"); // Prints a generic message for animal sound
    }
}
